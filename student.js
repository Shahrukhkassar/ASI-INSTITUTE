function render(){let tests=JSON.parse(localStorage.getItem('asi_tests')||'[]');document.getElementById('tests').textContent=tests.length;document.getElementById('testList').innerHTML=tests.length?tests.map(t=>`<div class="test-card"><div><b>${t.title}</b><br><small>${t.exam} • ${t.subject} • ${t.duration} min</small></div><a class="btn" href="test.html?id=${t.id}">Start Test</a></div>`).join(''):'<p class="muted">No tests published yet.</p>'}render();